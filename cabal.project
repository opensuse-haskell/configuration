-- cabal.project

tests: true

packages:
  ./cabal2obs.cabal

optional-packages:
  ../distribution-opensuse/distribution-opensuse.cabal
-- Disabled until Cabal 3.2.x becomes available on Hackage.
--  ../cabal2spec/cabal2spec.cabal
