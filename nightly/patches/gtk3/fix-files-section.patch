Index: ghc-gtk3.spec
===================================================================
--- ghc-gtk3.spec	(revision 5)
+++ ghc-gtk3.spec	(working copy)
@@ -93,6 +93,8 @@
 %files -f %{name}.files
 %defattr(-,root,root,-)
 %doc COPYING
+%dir %{_datadir}/%{pkg_name}-%{version}
+%{_datadir}/%{pkg_name}-%{version}/*
 
 %files devel -f %{name}-devel.files
 %defattr(-,root,root,-)

