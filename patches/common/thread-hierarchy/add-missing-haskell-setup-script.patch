Index: ghc-thread-hierarchy.spec
===================================================================
--- ghc-thread-hierarchy.spec	(revision 2)
+++ ghc-thread-hierarchy.spec	(working copy)
@@ -51,6 +51,8 @@
 
 %prep
 %setup -q -n %{pkg_name}-%{version}
+echo > Setup.hs 'import Distribution.Simple'
+echo >>Setup.hs 'main = defaultMain'
 
 %build
 %ghc_lib_build

