Index: ghc-records-sop.spec
===================================================================
--- ghc-records-sop.spec	(revision 2)
+++ ghc-records-sop.spec	(working copy)
@@ -55,6 +55,8 @@
 
 %prep
 %setup -q -n %{pkg_name}-%{version}
+echo > Setup.hs 'import Distribution.Simple'
+echo >>Setup.hs 'main = defaultMain'
 
 %build
 %ghc_lib_build

