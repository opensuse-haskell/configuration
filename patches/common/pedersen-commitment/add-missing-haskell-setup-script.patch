Index: ghc-pedersen-commitment.spec
===================================================================
--- ghc-pedersen-commitment.spec	(revision 1)
+++ ghc-pedersen-commitment.spec	(working copy)
@@ -59,6 +59,8 @@
 
 %prep
 %setup -q -n %{pkg_name}-%{version}
+echo > Setup.hs 'import Distribution.Simple'
+echo >>Setup.hs 'main = defaultMain'
 
 %build
 %ghc_lib_build

