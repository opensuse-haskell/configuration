Index: ghc-websockets-rpc.spec
===================================================================
--- ghc-websockets-rpc.spec	(revision 1)
+++ ghc-websockets-rpc.spec	(working copy)
@@ -64,6 +64,8 @@
 
 %prep
 %setup -q -n %{pkg_name}-%{version}
+echo > Setup.hs 'import Distribution.Simple'
+echo >>Setup.hs 'main = defaultMain'
 
 %build
 %ghc_lib_build

