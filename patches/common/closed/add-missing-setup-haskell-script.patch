Index: ghc-closed.spec
===================================================================
--- ghc-closed.spec	(revision 2)
+++ ghc-closed.spec	(working copy)
@@ -57,6 +57,8 @@
 
 %prep
 %setup -q -n %{pkg_name}-%{version}
+echo > Setup.hs 'import Distribution.Simple'
+echo >>Setup.hs 'main = defaultMain'
 
 %build
 %ghc_lib_build

