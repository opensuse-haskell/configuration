Index: ghc-direct-sqlite.spec
===================================================================
--- ghc-direct-sqlite.spec	(revision 2)
+++ ghc-direct-sqlite.spec	(working copy)
@@ -40,5 +40,6 @@
 BuildRequires:  ghc-temporary-devel
 %endif
+BuildRequires:  sqlite3-devel
 
 %description
 This package is not very different from the other SQLite3 bindings out there,
@@ -58,5 +59,6 @@
 Requires(post): ghc-compiler = %{ghc_version}
 Requires(postun): ghc-compiler = %{ghc_version}
+Requires:       sqlite3-devel
 
 %description devel
 This package provides the Haskell %{pkg_name} library development files.
@@ -67,6 +69,7 @@
 
 
 %build
+%define cabal_configure_options -fsystemlib
 %ghc_lib_build
 
 

