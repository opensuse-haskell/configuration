Index: ghc-microlens-aeson.spec
===================================================================
--- ghc-microlens-aeson.spec	(revision 3)
+++ ghc-microlens-aeson.spec	(working copy)
@@ -60,6 +60,8 @@
 
 %prep
 %setup -q -n %{pkg_name}-%{version}
+echo > Setup.hs 'import Distribution.Simple'
+echo >>Setup.hs 'main = defaultMain'
 
 %build
 %ghc_lib_build

