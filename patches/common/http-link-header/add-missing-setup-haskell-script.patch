Index: ghc-http-link-header.spec
===================================================================
--- ghc-http-link-header.spec	(revision 1)
+++ ghc-http-link-header.spec	(working copy)
@@ -60,6 +60,8 @@
 
 %prep
 %setup -q -n %{pkg_name}-%{version}
+echo > Setup.hs 'import Distribution.Simple'
+echo >>Setup.hs 'main = defaultMain'
 
 
 %build

